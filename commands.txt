git init => initialize to repo

git status => check for untracked files

git add -A

git commit -m "mesg"

to add repo: git remote add (name) (copy path)

git push -u (name) (branch): to push

git remote -v : check
